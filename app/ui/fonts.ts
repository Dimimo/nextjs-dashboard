import { Inter } from 'next/font/google';
import { Roboto} from "next/font/google";

export const inter = Inter({ weight: ['400', '700'], subsets: ['latin']});
export const roboto = Roboto({ weight: ['400', '700'], subsets: ['latin', 'math']});